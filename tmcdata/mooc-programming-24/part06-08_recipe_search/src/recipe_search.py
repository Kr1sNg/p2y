# Write your solution here
def ft_read(path):
	recipe_book = {} # name_recipe: int(time), ingredient1, ing2...
	with open(path) as file:
		for line in file:
			if line == 
			line = line.replace("\n", "")
	return ()
		
def	search_by_name(filename: str, word: str):
	
def search_by_time(filename: str, prep_time: int):
	recipes_book = ft_read(filename)



